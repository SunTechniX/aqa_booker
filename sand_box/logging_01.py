# В любом файле проекта:
import logging

# ✅ Правильно: имя = __name__ (модуль) или __class__.__name__ (класс)
logger = logging.getLogger(__name__)  # для модуля

def test_log_01():
    # Использование:
    logger.debug("Детали для отладки")      # Только при уровне DEBUG
    logger.info("Шаг теста: переход на /login")  # Стандартный ход
    logger.warning("⚠️ Предупреждение: элемент грузится медленно")  # Не критично, но стоит знать
    logger.error("❌ Ошибка: не удалось найти элемент")  # Тест, скорее всего, упадёт
    logger.exception("💥 Исключение при выполнении")  # + стек вызовов (внутри except)
